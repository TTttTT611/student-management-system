-- Create the database on first use; tables are created by Alembic (alembic upgrade head)
CREATE DATABASE IF NOT EXISTS student_db
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;
